reference_case: ref_case

reference_case_path:
  - path/to/reference/case

reference_case_file_format: history

reference_case_catalog_json: reference_case_catalog.json

case_to_compare:
  - some_case

case_to_compare_paths:
  - path/to/data

case_to_compare_file_format: timeseries

case_to_compare_catalog_json: case_to_compare_catalog.json
